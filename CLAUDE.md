# CLAUDE.md — 项目工作指引

> 本文件为开发协作的总入口,先读这里,再按需查阅 `docs/` 下文档。

## 1. 项目简介

**HistoryCopy**:运行在 Windows 上的历史剪贴板软件。后台常驻监听复制,
记录文字和图片,支持时间倒序浏览、搜索、置顶、删除,可设置保留期限(1/3/5 天),
打包为独立 exe,淡蓝色简洁界面。用户为不懂代码的普通用户,沟通需通俗易懂。

## 2. 文档索引(标准文件路径)

| 文档 | 路径 | 用途 |
|---|---|---|
| 需求文档 | `docs/requirements.md` | 功能与验收标准,变更先改这里 |
| 技术方案 | `docs/tech-spec.md` | 技术选型、项目结构、数据设计、关键机制 |
| 界面设计规范 | `docs/design-spec.md` | 色板、字体、布局、交互规范 |
| 开发计划 | `docs/dev-plan.md` | 分阶段任务与验收点,完成时勾选 |
| 开发日志 | `devlog/` | 每日开发记录,格式见 `devlog/README.md` |

## 3. 工作说明(每次开发前必读)

### 3.1 开发流程

1. **读取文档**:先读 `docs/dev-plan.md` 确认当前阶段,再读对应技术/设计文档
2. **建立/更新日志**:打开 `devlog/` 当日文件,记录待办与完成
3. **小步实施**:一次只做一个阶段(见开发计划),不跳跃、不批量堆功能
4. **验证**:每阶段完成按验收点验证,并在日志中记录结果
5. **保持程序可运行**:每阶段结束时程序状态是稳定的,不留下半成品

### 3.2 每日日志规则

- 每工作日在 `devlog/` 新建 `YYYY-MM-DD.md`(格式见 `devlog/README.md`)
- 会话开始先写"今日待办";每完成一项勾选;会话结束写"明日计划"
- 自动记录:每个开发会话的完成事项与待办必须写入当日日志,不得遗漏

### 3.3 变更管理

- 需求/方案任何变更:先更新 `docs/` 对应文档并注明原因,再实施
- 变更后须向用户说明,得到确认

## 4. 技术约定(实现时遵循)

- 语言:Python 3.11+;界面:PySide6;存储:SQLite + 图片文件;打包:PyInstaller
- 目录结构、数据表结构、关键机制:见 `docs/tech-spec.md`
- UI 色板/字体/布局:见 `docs/design-spec.md`,不得偏离
- 运行环境:Windows 10+,最低兼容 8.1 目标(以 Windows 10 为准)

## 5. 与用户沟通约定

- 用户是代码小白:解释要通俗,不用专业术语堆砌
- 每次做完一个阶段,用一句话说明"做了什么、你可以怎么验证",等确认再继续
- 涉及"后台常驻""开机自启""注册表"等敏感操作,先说明再实施

## 6. 完成标准

满足 `docs/requirements.md` 第 7 节"验收标准"后,本项目方视为完成。
