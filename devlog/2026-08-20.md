# 开发日志 2026-08-20

## 今日待办
- [x] 排查并修复卡片/列表被撑宽问题(删除按钮超出列表容器)
- [x] 卡片宽度的两层防护:断行方案完善 + 容器宽度约束兜底
- [x] 更新 docs/(tech-spec、design-spec、learn-tech)与当日日志
- [x] 重新打包 exe 交用户验证
- [x] 代码体检:冗余文件清理 + 打包统一 + 剪贴板图片增强(PNG/DIBV5)+ 监听串行化 + 句柄泄漏 + OCR 状态保留

## 今日完成
- [x] 复现并定位根因:列表容器宽度 = 所有卡片最小宽度的最大值;只要任意一条文字含**不换行空格**(U+00A0/U+2007/U+202F,网页/Office 复制常见),旧 `_break_long_tokens` 会把它们当普通空白重置零宽空格计数,而 QLabel 又不在它们处折行 → 整段变成一个无法断行的超长单词(实测容器被一条 480 字符记录从 352px 撑到 1224px)→ 所有卡片一起变宽,删除按钮超出可视区。
- [x] `src/ui/card.py`:`_break_long_tokens` 不再把不换行空格当断行点,按普通字符计长并在其后补零宽断行符(实测该记录下卡片最小宽度回落正常)。
- [x] `src/ui/main_window.py`:列表布局加 `setSizeConstraint(QLayout.SetNoConstraint)`,列表容器永远不超过可视区宽度;极端内容仅卡片内截断(悬停可看全文),从机制上杜绝"容器被撑宽"整类问题。
- [x] 验证:真实 `MainWindow` 拉起 + 含不换行空格长文本记录,容器 = 视口宽度(354px),各卡片宽度 348px、删除按钮右缘 338px(留白正常)。
- [x] 文档更新:tech-spec 新增 §4.7 卡片宽度防护;design-spec 新增 §5.4 宽度保障;learn-tech 补充第二次"卡片被撑破"记录与教训。
- [x] **打包统一**:`build.bat` 改为调用 `HistoryCopy.spec`(原命令行方式不带 OCR 模型,会悄悄打包出无识别功能的 exe);`.gitignore` 取消忽略 `HistoryCopy.spec`(打包配置入库);README/tech-spec 打包说明同步。
- [x] **剪贴板图片增强**:`get_clipboard_image` 依次尝试 DIB → DIBV5 → PNG(注册格式),现代浏览器/Edge 只提供 PNG 的"复制图片"也能记录;已用真实剪贴板验证 PNG 回读成功。
- [x] **监听串行化**:`ClipboardMonitor` 改为单 worker 常驻线程消费唤醒事件(短时间多次变化合并),避免"每次变化新开线程"的堆积与并发读剪贴板偶发漏读;自检(self_test)全部通过。
- [x] **句柄泄漏清理**:`set_clipboard_text/image` 在 `GlobalLock`/`SetClipboardData` 失败路径补 `GlobalFree`,移除 `finally: pass` 死代码。
- [x] **OCR 状态保留**:列表刷新时对识别中的图片卡恢复"识别中…"禁用状态(实测通过)。
- [x] 冗余清理:删除根目录 `build/` 中间产物、`src/**/__pycache__/`、`data/images/` 两个测试残留图;`dist/data/`(真实运行数据)保留不动。
- [x] **追加修复(卡片宽度第二轮)**:真实数据复现后确认上一轮 `SetNoConstraint` 并未真正拦住"置顶图片卡"把容器撑宽;根因是**底栏一行 = 时间 + 5 个按钮,最小约 375px > 420px 窗口可视区 346px**(旧时间戳"2026-08-11 21:49"宽 85px)。实测:置顶图片卡最小 387px、容器被撑到 391px、删除按钮右缘 379px 被裁掉。
  - 根因验证实验:`SizePolicy.Ignored` 能强制容器=视口(352px),但按钮行最小宽仍 375px > 346px,单靠它删除按钮仍会被顶出 → 必须同时"压缩底栏需求宽度"。
  - `src/ui/card.py`:时间标签从底部按钮行挪到**头部行右侧**(置顶徽标在左、时间在右);底栏间距 8→6,按钮行只留按钮。
  - `src/ui/theme.py`:`btnMini / btnMiniDanger / btnOcr` 水平内边距 10px→8px(三个选择器,视觉微调)。
  - `src/utils.py`:`format_time` 非今/昨日期改为 `MM-DD HH:MM`(省略年份,时间更短)。
  - `src/ui/main_window.py`:列表内容容器横向 `SizePolicy.Ignored`,宽度恒定=视口,兜底杜绝未来任何宽度问题。
  - 验证(真实 MainWindow + 真实 `dist/data/history.db`,420px):`viewport=346, container=346`,最宽卡片 342px、删除按钮右缘 334px **完全可见**(余量 12px);竖滚动条存在时同样成立。
  - 重新打包:H 版本 `HistoryCopy.exe` 已用 `python -m PyInstaller HistoryCopy.spec` 重建。
  - 文档:design-spec §5.1/5.2 卡片示意图与 §5.4 宽度保障、learn-tech §8.4 卡片结构与教训段同步更新。
  - 教训:`SetNoConstraint` 只管布局自身最小宽,**拦不住单个子项(按钮行)的本地最小宽**;一行放不下就换行/换位置,别硬塞。

## 遗留问题 / 阻塞
- 无。用户在 420px 窗口双击运行新版 exe,界面效果确认无问题;已在 dev-plan「完成状态」勾选 UI 宽度稳定性验收项。

## 明日计划
- 按用户实测反馈迭代;若用户确认无问题,在 dev-plan 中勾选"UI 稳定"相关验收项